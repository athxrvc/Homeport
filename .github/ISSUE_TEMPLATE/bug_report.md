---
name: Bug report
about: Something doesn't work as the docs say it should
title: ""
labels: bug
---

**Before you post:** remove your API key (`sk-...`), tunnel token and tunnel URL from anything you paste.

## What happened

<!-- What did you do, what did you expect, and what happened instead? -->

## Steps to reproduce

1.
2.
3.

## Your setup

- OS and version (e.g. Windows 11, macOS 15, Ubuntu 24.04):
- Shell (Windows PowerShell 5.1 / PowerShell 7 / bash / zsh):
- Command you ran (e.g. `.\scripts\start.ps1 -Tunnel named`):
- Tunnel type (quick / named / Tailscale / none):
- Versions (paste the output of each):
  - `ollama --version`:
  - `pip show litellm` (Version line):
  - `cloudflared --version`:
  - `python --version`:

## Logs

<!-- The script's error message, plus the last lines of the relevant files in logs/ (litellm.err.log, cloudflared.err.log). -->

```text

```

## Anything else

<!-- Does it work with `-Tunnel none`? Does a direct request to Ollama (http://127.0.0.1:11434) work? -->
